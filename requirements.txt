# ══════════════════════════════════════════════════════════════
# AzaBot v4 — Production Requirements
# Python 3.11 | Rasa Pro CALM 3.16
#
# التثبيت:
#   pip install -r requirements.txt
#
# ملاحظة: rasa-pro يجلب تلقائياً:
#   tensorflow, torch, transformers, scipy, numpy,
#   sqlalchemy, alembic, redis, grpcio, protobuf,
#   وكل dependencies الثقيلة الأخرى
# ══════════════════════════════════════════════════════════════

# ── Rasa Pro CALM ─────────────────────────────────────────────
rasa-pro[nlu]>=3.16,<3.17
rasa-sdk>=3.16,<3.17

# ── Web Framework ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── HTTP Client ───────────────────────────────────────────────
httpx>=0.27.0

# ── Data Validation ───────────────────────────────────────────
pydantic>=2.0.0

# ── AI / LLM ─────────────────────────────────────────────────
openai>=1.0.0

# ── Database ──────────────────────────────────────────────────
supabase>=2.0.0
PyJWT>=2.8.0

# ── System ────────────────────────────────────────────────────
psutil>=5.9.0
pyyaml>=6.0.0

# ── Auth / Security ───────────────────────────────────────────
bcrypt>=4.0.0

# ── Google Auth (مطلوب من rasa-pro) ───────────────────────────
# تثبيت صريح لمنع conflict مع google-cloud-aiplatform

# ── Security additions (v4.1) ─────────────────────────────────
bcrypt>=4.1.0
redis>=5.0.0
