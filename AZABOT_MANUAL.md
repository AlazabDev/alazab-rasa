# 📘 الدليل الشامل لمشروع AzaBot v4.1 (التسطيب، التدريب، التشغيل، والنشر)

هذا الدليل يحتوي على الخطوات الكاملة من الصفر حتى النشر على خادم الإنتاج الخاص بك.

---

## 🛠️ أولاً: التسطيب وإعداد البيئة (Setup)

### 1. إعداد المتطلبات والمجلدات
في مجلد المشروع الرئيسي، قم بتشغيل أمر الإعداد المسبق الذي سيقوم بتثبيت الحزم ونسخ ملف `.env`:
```bash
make setup
```
*(هذا الأمر سيقوم بتنفيذ `pip install -r requirements.txt` وإنشاء المجلدات اللازمة للتخزين)*

### 2. إعداد المتغيرات البيئية (`.env`)
افتح ملف `.env` وقم بتعبئة البيانات الأساسية:
- **Rasa Pro License:** `RASA_PRO_LICENSE`
- **OpenAI:** `OPENAI_API_KEY`
- **Supabase:** `SUPABASE_URL` و `SUPABASE_DB_PASSWORD`
- **تشفير البيانات:** `ENCRYPTION_KEY`

### 3. إعداد كلمات المرور وجلسات الإدارة
لتوليد كلمات مرور قوية ومُشفرة (Bcrypt) للمديرين، قم بتشغيل:
```bash
python scripts/gen_password_hash.py
```
انسخ الـ Hash الناتج وضعه في `.env` أمام المستخدم المناسب (مثلاً `ADMIN_PASSWORD_HASH_ADMIN`).
ولتوليد سر الجلسات `ADMIN_SESSION_SECRET`:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 4. تهيئة قاعدة البيانات (Supabase)
```bash
make db-init
```

---

## 🧠 ثانياً: تدريب نموذج الذكاء الاصطناعي (Training)

لضمان عدم وجود أي أخطاء أثناء التدريب، تم إعداد أوامر خاصة:

### 1. الفحص الاستباقي (Pre-Train Check)
قم بإجراء فحص شامل للبيانات، الاتصالات، ومفاتيح الـ API:
```bash
make check
```
*(إذا ظهرت أي رسالة خطأ باللون الأحمر ❌، يجب إصلاحها قبل المتابعة)*

### 2. بدء التدريب
```bash
make train
```
*(هذا الأمر سيقوم أولاً بعمل `rasa data validate` ثم سيبدأ التدريب ويحفظ الموديل في مجلد `models/`)*

---

## 🚀 ثالثاً: التشغيل المحلي والتطوير (Local Run)

لبدء تشغيل جميع الخدمات (Action Server, Rasa Server, Webhook Server) معاً:
```bash
make run
```
أو عبر سكربت التشغيل الموحد:
```bash
bash run.sh
```
- لوحة تحكم الإدارة ستكون متاحة على: `http://127.0.0.1:8000/admin`
- حالة الخدمات (Health): `http://127.0.0.1:8000/health/details`

---

## 🌍 رابعاً: النشر على بيئة الإنتاج (Production Deployment)

نظام النشر يعتمد بشكل كامل على تشغيل الخادم كخدمات `systemd` خلفية باستخدام سكربت جاهز على نظام (Ubuntu 22.04+).

### 1. الاستعداد للنشر
- تأكد أن السيرفر الحي يحتوي على Python 3.10 أو أحدث.
- تأكد أن النطاق (مثلاً `bot.alazab.com`) يشير إلى عنوان IP الخاص بالسيرفر.
- انسخ ملف `.env` واضبط `NODE_ENV=production`.

### 2. تشغيل سكربت النشر الإنتاجي
السكربت التالي سيقوم بإنشاء المستخدم الخاص بالخدمة (`azab`)، إعداد `Redis`، بناء الـ Frontend، إعداد `Nginx` مع `certbot`، وتجهيز `systemd`:
```bash
sudo bash deploy/production/deploy-production.sh
```

### 3. الأوامر الهامة في بيئة الإنتاج
بعد نجاح النشر، يمكنك التحكم في البوت باستخدام أوامر لينكس القياسية:

- **مراقبة السجلات الحية (Logs):**
  ```bash
  sudo journalctl -u azabot-webhook -f
  sudo journalctl -u azabot-rasa -f
  ```
- **إعادة تشغيل النظام:**
  ```bash
  sudo systemctl restart azabot-webhook azabot-rasa azabot-actions
  ```
- **التدريب على الإنتاج:**
  نظراً لتحديث Webhook، يمكنك بدء التدريب مباشرة من لوحة تحكم الأدمن (من واجهة الـ Frontend) وسيتم تشغيله في الخلفية دون إسقاط الخادم.

---
*تلميح: احتفظ بهذا الملف للرجوع إليه عند الحاجة لتهيئة خوادم جديدة أو إضافة مطورين جدد لفريق العمل.*
