# ══════════════════════════════════════════════════════════════
# المرحلة 2 — حزم AzaBot الإضافية
# تُثبَّت بعد rasa-pro فتُحل فقط ضد بيئة مُثبَّتة (مش resolve مشترك)
# ⚠️ لا تكتب fastapi/uvicorn/pydantic/httpx هنا بحد أدنى مفتوح —
#    rasa-pro يثبّتها بالفعل؛ لو احتجت رفع إصدار حدد سقف صريح
# ══════════════════════════════════════════════════════════════
python-multipart>=0.0.9,<0.1
python-dotenv>=1.0.0,<2.0

openai>=1.0.0,<2.0

supabase>=2.0.0,<3.0
PyJWT>=2.8.0,<3.0

psutil>=5.9.0,<6.0
redis>=5.0.0,<6.0
bcrypt>=4.0.0,<5.0
